# Agent-less perimeter telemetry is hunted via a dedicated Infrastructure organisation

The whole platform attributes a Wazuh event to a tenant by exactly one mechanism: `agent.id` ∈ that org's `wazuh_group` (the **Tenant-isolation invariant** in CONTEXT.md, reaffirmed in [ADR-0015](0015-threat-hunting-incident-producing-module.md)). But the perimeter sources shared across all tenants — a single **firewall** and a single **reverse proxy** — log against the Wazuh **manager** (`agent.id = "000"`), so they carry no per-org attribution. Threat hunting must reach this telemetry (it is essential evidence), yet every Hunt lens hard-filters on per-org `agent.id` and therefore could never see it. We make these **Shared Infrastructure events** first-class by giving them a home: a dedicated, non-tenant `Organization`.

## Decision

- **Introduce an Infrastructure organisation** — a real `Organization` row flagged `is_infrastructure = True`. It is the single home for Shared Infrastructure events: infra **Findings** carry it as their `organization`, so they ride the *unchanged* propose-and-confirm bridge and **materialise into an Incident in the Infrastructure org**. A real, agent-bound tenant's incidents therefore never contain mis-attributed perimeter events.
- **Select agent-less events with a *positive* filter `agent.id = "000"`** — never by dropping the agent filter (the Scheduled-Search `include_agentless`/`effective_agent_ids = None` approach). Dropping the filter would match every tenant's documents in one un-attributed query; a positive term keeps the query bounded to the shared-infra bucket. Mechanically this rides the existing per-scope fan-out: the Infrastructure org resolves to `agent_ids = ["000"]` (special-cased, **not** via `get_agents(wazuh_group)`), so the IOC sweep and the aggregation lenses include it with no lens changes. The per-host lenses (`agent_activity`/`processes`/`ports`) do not apply and degrade to their existing "out of scope" path.
- **Included only by Hunt scope resolution, invisible everywhere else.** All "real tenant" loops — System Search Rule fan-out, fleet vulnerability scans, per-org incident tasks, billing/dashboards/tenant counts — use a tenants-only queryset (`is_infrastructure = False`) so the pseudo-org never leaks in. An *all-orgs* Hunt includes it automatically; it is additionally selectable on its own in the **staff** hunt-scope picker (an opt-in `?include_infrastructure=1` on the org-list endpoint, so it stays out of the incident-assignment / tenant-management pickers).

## Considered Options

- **Nullable `HuntFinding.organization` + non-materialisable "evidence-only" bucket** — surface infra hits as read-only context the analyst pivots from, never an incident. Touches far fewer subsystems (no flag, no audit of `Organization.objects.all()` sites) and keeps the tenant table free of a fake row, but infra findings can never be promoted to an incident. Rejected: perimeter findings are sometimes the actionable thing, and we want the standard materialise/track lifecycle for them.
- **Manual attribution on confirm** — let the analyst pick a real tenant to attach an infra finding to. Rejected for v1: it deliberately writes shared-infra evidence into one tenant's incident, re-introducing the mis-attribution the Infrastructure org is meant to prevent (can be layered on later as an explicit "also attach to org X" action).
- **Drop the agent filter (reuse `include_agentless` semantics) for all-orgs hunts** — rejected: an agent-filter-less query reads across *all* tenants at once, the exact cross-tenant join the hunt module forbids; and it still leaves the result with no org to attach findings to.

## Consequences

- A schema change (`Organization.is_infrastructure`) and a one-time audit converting the six existing `Organization.objects.all()` "real tenant" loops to a tenants-only queryset. Missing one is a silent correctness bug (e.g. a System Rule double-running, or the pseudo-org appearing in billing), so the tenants-only accessor should be the *default* and `all()` the explicit exception.
- This ADR records a **sanctioned, narrow breach** of the Tenant-isolation invariant: one `Organization` deliberately does not map to a `wazuh_group`. Future readers seeing a customer-less org or a hard-coded `agent.id = "000"` should find the reason here.
- `agent.id = "000"` also captures Wazuh's own manager-internal events (FIM/rootcheck on the manager), not only firewall + reverse proxy. Acceptable as extra telemetry for a hunt; narrowing the Infrastructure bucket to specific decoders/locations is a future refinement if the noise proves unhelpful.
- Incidents can now exist in a non-tenant org; any staff incident view/list must tolerate that (it is still a normal `Incident`, `source_kind = threat_hunt`).
