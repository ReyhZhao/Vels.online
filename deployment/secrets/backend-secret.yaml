# Template only — do not commit real values.
# Your Kubernetes secrets provider should populate the backend-secret Secret
# with the keys below. This file documents the required structure.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: vels-online
type: Opaque
stringData:
  SECRET_KEY: "replace-with-django-secret-key"
  DATABASE_URL: "postgres://user:password@host:5432/vels_online"
  AUTHENTIK_CLIENT_ID: "replace-with-client-id"
  AUTHENTIK_CLIENT_SECRET: "replace-with-client-secret"
  AUTHENTIK_SERVER_URL: "https://authentik.example.com/application/o/vels-online/"
